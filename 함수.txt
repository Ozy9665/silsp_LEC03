함수
어떤 특정한 일을 처리하는 기능을 모아놓은 것, 수학적인 함수도 구현 가능.
쓰는 이유 - 재사용하기 위해
라이브러리, 모듈은 여러개의 함수들로 구성됨. (모듈.함수) ex) turtle.forward(100)
프로그래머는 자기만의 함수를 만들 수 있음.
함수의 이름은 그 함수의 기능을 정확히 나타내는 것이 좋음.

문법: 함수 정의 – 함수를 만들기


r,_ = sum_and_mul(4,5)     => _는 무시
r = 9